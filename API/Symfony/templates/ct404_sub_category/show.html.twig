{% extends 'base.html.twig' %}

{% block title %}Ct404SubCategory{% endblock %}

{% block body %}
    <h1>Ct404SubCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct404_sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ct404_sub_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct404_sub_category_index') }}">back to list</a>

    <a href="{{ path('ct404_sub_category_edit', {'id': ct404_sub_category.id}) }}">edit</a>

    {{ include('ct404_sub_category/_delete_form.html.twig') }}
{% endblock %}
