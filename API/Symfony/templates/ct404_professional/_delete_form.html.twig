<form method="post" action="{{ path('ct404_professional_delete', {'id': ct404_professional.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct404_professional.id) }}">
    <button class="btn">Delete</button>
</form>
