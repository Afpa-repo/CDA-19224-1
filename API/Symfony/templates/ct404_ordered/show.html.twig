{% extends 'base.html.twig' %}

{% block title %}Ct404Ordered{% endblock %}

{% block body %}
    <h1>Ct404Ordered</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct404_ordered.id }}</td>
            </tr>
            <tr>
                <th>OrderDate</th>
                <td>{{ ct404_ordered.orderDate ? ct404_ordered.orderDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeliveryDate</th>
                <td>{{ ct404_ordered.deliveryDate ? ct404_ordered.deliveryDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ ct404_ordered.totalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct404_ordered_index') }}">back to list</a>

    <a href="{{ path('ct404_ordered_edit', {'id': ct404_ordered.id}) }}">edit</a>

    {{ include('ct404_ordered/_delete_form.html.twig') }}
{% endblock %}
