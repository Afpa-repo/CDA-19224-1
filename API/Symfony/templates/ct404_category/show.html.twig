{% extends 'base.html.twig' %}

{% block title %}Ct404Category{% endblock %}

{% block body %}
    <h1>Ct404Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct404_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ct404_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct404_category_index') }}">back to list</a>

    <a href="{{ path('ct404_category_edit', {'id': ct404_category.id}) }}">edit</a>

    {{ include('ct404_category/_delete_form.html.twig') }}
{% endblock %}
